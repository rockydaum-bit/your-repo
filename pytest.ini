[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
	integration: tests that hit external services (disabled by default)
    acceptance: end-to-end acceptance tests (stubbed IO / deterministic)
filterwarnings =
	ignore::DeprecationWarning:httplib2.*
